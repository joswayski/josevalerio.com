version: '3'
services:
  app:
    restart: always
    image: joswayski/josevalerio:latest
    # ports:
    #   - "3000:3000"
    environment:
      NODE_ENV: production
#     depends_on:
#       - postgres
#   postgres:
#     image: postgres:latest
#     environment:
#       POSTGRES_USER: ${DB_USER}
#       POSTGRES_PASSWORD: ${DB_PASS}
#     volumes:
#       - postgres-data:/var/lib/postgresql/data
# volumes:
#   postgres-data:

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443" # Map Nginx to the standard HTTPS port
    volumes:
      - ./myapp.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - app
